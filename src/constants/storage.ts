// Browser-storage keys shared between the component that writes them and the auth slice that
// resets them on sign-in/sign-out — kept here so the two can never drift apart.
export const SIDEBAR_COLLAPSED_KEY = 'sbr_sidebar_collapsed';
export const SIDEBAR_GROUPS_KEY = 'sbr_sidebar_groups';
