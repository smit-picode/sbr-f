import { ApprovalsHistoryPage } from '@/features/approvals/pages/ApprovalsHistoryPage';

export const metadata = { title: 'Approvals History — SBR Portal' };

export default function Page() {
  return <ApprovalsHistoryPage />;
}
