import { AdminTabPage } from '@/features/admin/pages/AdminTabPage';

export const metadata = { title: 'Permissions — SBR Portal' };

export default function Page() {
  return <AdminTabPage tab="permissions" />;
}
