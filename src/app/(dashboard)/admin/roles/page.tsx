import { AdminTabPage } from '@/features/admin/pages/AdminTabPage';

export const metadata = { title: 'Roles — SBR Portal' };

export default function Page() {
  return <AdminTabPage tab="roles" />;
}
