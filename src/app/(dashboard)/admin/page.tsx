import { AdminIndexPage } from '@/features/admin/pages/AdminIndexPage';

export const metadata = { title: 'Admin Panel — SBR Portal' };

export default function Page() {
  return <AdminIndexPage />;
}
