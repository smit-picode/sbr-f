import { BulkChangeListPage } from '@/features/bulkChange/pages/BulkChangeListPage';

export const metadata = { title: 'Bulk Change — SBR Portal' };

export default function Page() {
  return <BulkChangeListPage />;
}
