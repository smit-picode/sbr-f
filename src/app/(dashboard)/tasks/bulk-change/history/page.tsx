import { BulkChangeHistoryPage } from '@/features/bulkChange/pages/BulkChangeHistoryPage';

export const metadata = { title: 'Bulk Change History — SBR Portal' };

export default function Page() {
  return <BulkChangeHistoryPage />;
}
