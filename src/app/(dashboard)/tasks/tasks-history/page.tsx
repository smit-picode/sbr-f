import { TasksHistoryPage } from '@/features/tasks/pages/TasksHistoryPage';

export const metadata = { title: 'Tasks History — SBR Portal' };

export default function Page() {
  return <TasksHistoryPage />;
}
