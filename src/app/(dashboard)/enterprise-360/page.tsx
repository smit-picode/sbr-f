import { Enterprise360Page } from '@/features/enterprise360/pages/Enterprise360Page';

export const metadata = { title: 'Enterprise 360 — SBR Portal' };

export default function Page() {
  return <Enterprise360Page />;
}
