import { ContactsListPage } from '@/features/contacts/pages/ContactsListPage';

export const metadata = { title: 'Contacts — SBR Portal' };

export default function Page() {
  return <ContactsListPage />;
}
