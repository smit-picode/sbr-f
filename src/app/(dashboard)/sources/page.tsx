import { SourceCatalogPage } from '@/features/sourceData/pages/SourceCatalogPage';

export const metadata = { title: 'Source Catalog — SBR Portal' };

export default function Page() {
  return <SourceCatalogPage />;
}
